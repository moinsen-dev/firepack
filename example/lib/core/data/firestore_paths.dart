// Hand-written stub — referenced by every generated repository.
//
// firepack today does NOT generate this file (despite the README hint —
// see open issue: paths.dart codegen). The list of collections lives in
// `example/firepack.yaml` and must be mirrored here by hand.
//
// When firepack lands paths.dart codegen, this file becomes generated
// alongside models/ and repositories/, and consumers stop hand-writing
// it.
class FirestorePaths {
  FirestorePaths._();

  static const String organizations = 'organizations';
  static const String users = 'users';
  static const String posts = 'posts';
  static const String comments = 'comments';
}
